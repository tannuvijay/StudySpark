{
  "name": "ai-study-assistant-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:backend": "cd backend && mvn spring-boot:run",
    "start:frontend": "cd frontend && npm run dev",
    "install:frontend": "cd frontend && npm install"
  },
  "dependencies": {
    "jwt-decode": "^4.0.0"
  }
}
